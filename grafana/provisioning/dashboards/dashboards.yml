apiVersion: 1

providers:
  - name: Chrono Queue Dashboards
    orgId: 1
    folder: Chrono Queue
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
